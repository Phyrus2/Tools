# Dependencies
node_modules/
# Local environment files at any directory depth; only the dummy template is public.
.env
.env.*
!.env.example
# Local data and tunnel credentials
uploads/
cloudflare/*.exe
cloudflare/*.json
cloudflare/*.pem
.cloudflared/
# Runtime state, logs, and temporary database backups from server scripts
.server-state/
.server-logs/
.server-tools/
rclone.conf
